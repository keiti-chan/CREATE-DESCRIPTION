# CREATE-DESCRIPTION

Static description builder prototype for KFK product and bundle descriptions.

Open `index.html` directly in a browser to use the app. Templates are split under `templates/` so product and bundle copy can be updated separately.
